import test from 'node:test';import assert from 'node:assert/strict';import * as THREE from 'three';
import {Promatrix106MachineTemplate} from '../frontend/src/promatrix106.js';import {Promatrix106ProcessSimulation,PROMATRIX106_SIMULATION_STAGES} from '../frontend/src/simulation-promatrix106.js';import {PROMATRIX106_SPEC,PROMATRIX106_SPEC_APM9} from '../frontend/src/data/dimensions-promatrix106.js';import {promatrix106TaxonomyFor} from '../frontend/src/data/taxonomy-promatrix106.js';import {PROMATRIX106_TECHNICAL_SOURCES} from '../frontend/src/data/sources-promatrix106.js';
test('Promatrix 106 CSB keeps BMJ identities separate from official family limits',()=>{assert.equal(PROMATRIX106_SPEC.assetId,'BMJ-MCH-0014');assert.equal(PROMATRIX106_SPEC.serial,'MP.DBE0-00100');assert.equal(PROMATRIX106_SPEC_APM9.assetId,'BMJ-MCH-0015');assert.equal(PROMATRIX106_SPEC_APM9.serial,'MP.DBE0-00115');assert.deepEqual(PROMATRIX106_SPEC.sheetMax,[.760,1.060]);assert.equal(PROMATRIX106_SPEC.cuttingPressureMN,2.6);assert.equal(PROMATRIX106_SPEC.cuttingPressureTonnes,260);assert.ok(PROMATRIX106_TECHNICAL_SOURCES.some(s=>s.authority==='primary'));assert.ok(PROMATRIX106_TECHNICAL_SOURCES.some(s=>s.id==='PROMATRIX-EVIDENCE-BOUNDARY'));});
test('both BMJ Promatrix assets instantiate the CSB station train',()=>{for(const assetId of ['BMJ-MCH-0014','BMJ-MCH-0015']){const model=new Promatrix106MachineTemplate(assetId),box=new THREE.Box3().setFromObject(model.root);assert.equal(model.root.userData.assetId,assetId);assert.ok(!box.isEmpty());assert.ok(box.min.y>=-0.01);for(const id of ['pm106-feeder','pm106-feedtable','pm106-transport','pm106-cutting','pm106-stripping','pm106-blanking','pm106-delivery','pm106-drive','pm106-access'])assert.ok(model.findNode(id),id);model.setExteriorOpen(true);assert.ok(model.root.userData.exteriorHiddenCount>=8);model.dispose();}});
test('Promatrix taxonomy is a six-level mapped tree for APM 8 and APM 9',()=>{for(const assetId of ['BMJ-MCH-0014','BMJ-MCH-0015']){const taxonomy=promatrix106TaxonomyFor(assetId);assert.deepEqual([...new Set(taxonomy.map(n=>n.level))].sort(),[1,2,3,4,5,6]);assert.equal(new Set(taxonomy.map(n=>n.id)).size,taxonomy.length);for(const n of taxonomy.filter(n=>n.level>1))assert.ok(taxonomy.some(p=>p.id===n.parentId),n.id);const model=new Promatrix106MachineTemplate(assetId);for(const n of taxonomy.filter(n=>n.verified&&n.meshRefs.length))assert.ok(model.resolveTaxonomyNode(n.id),n.id);model.dispose();}});
test('Promatrix simulation separates cutting, stripping, blanking and rake delivery',()=>{const model=new Promatrix106MachineTemplate(),sim=new Promatrix106ProcessSimulation(model.root,model);assert.ok(PROMATRIX106_SIMULATION_STAGES.includes('Non-stop rake delivery'));sim.start();sim.update(sim.startAt+4200);let state=sim.state();assert.equal(state.cuttingActive,true);sim.update(sim.startAt+6800);state=sim.state();assert.equal(state.strippingActive,true);sim.update(sim.startAt+9200);state=sim.state();assert.equal(state.blankingActive,true);sim.update(sim.startAt+11200);state=sim.state();assert.ok(state.completed>0);assert.ok(state.pileSheetsVisible>0);assert.ok(state.wastePiecesVisible>0);sim.dispose();model.dispose();});
