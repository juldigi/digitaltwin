import test from 'node:test';import assert from 'node:assert/strict';import * as THREE from 'three';
import {FZ1200MachineTemplate} from '../frontend/src/fz1200.js';import {FZ1200ProcessSimulation,FZ1200_SIMULATION_STAGES} from '../frontend/src/simulation-fz1200.js';import {FZ1200_ASSET_PROFILES} from '../frontend/src/data/dimensions-fz1200.js';import {fz1200TaxonomyFor} from '../frontend/src/data/taxonomy-fz1200.js';import {FZ1200_TECHNICAL_SOURCES} from '../frontend/src/data/sources-fz1200.js';
test('FZ1200 keeps all three BMJ identities without choosing an unverified OEM',()=>{assert.equal(Object.keys(FZ1200_ASSET_PROFILES).length,3);assert.equal(FZ1200_ASSET_PROFILES['BMJ-MCH-0007'].serial,'24RVOFS0920');assert.equal(FZ1200_ASSET_PROFILES['BMJ-MCH-0008'].serial,'2105080SF34');assert.equal(FZ1200_ASSET_PROFILES['BMJ-MCH-0022'].serial,'22000320');assert.ok(FZ1200_TECHNICAL_SOURCES.some(s=>s.id==='FZ1200-EVIDENCE-BOUNDARY'));});
test('each FZ1200 asset instantiates clamp turn air jog hydraulic geometry',()=>{for(const assetId of Object.keys(FZ1200_ASSET_PROFILES)){const model=new FZ1200MachineTemplate(assetId),box=new THREE.Box3().setFromObject(model.root);assert.equal(model.root.userData.assetId,assetId);assert.ok(!box.isEmpty());assert.ok(box.min.y>=-0.01);for(const id of ['fz1200-base','fz1200-clamp','fz1200-turn','fz1200-air','fz1200-jog','fz1200-hydraulic','fz1200-control'])assert.ok(model.findNode(id),id);model.dispose();}});
test('FZ1200 taxonomy is six-level for every BMJ unit',()=>{for(const assetId of Object.keys(FZ1200_ASSET_PROFILES)){const taxonomy=fz1200TaxonomyFor(assetId);assert.deepEqual([...new Set(taxonomy.map(n=>n.level))].sort(),[1,2,3,4,5,6]);assert.equal(new Set(taxonomy.map(n=>n.id)).size,taxonomy.length);for(const n of taxonomy.filter(n=>n.level>1))assert.ok(taxonomy.some(p=>p.id===n.parentId),n.id);}});
test('FZ1200 simulation clamps turns airs and jogs in ordered phases',()=>{const model=new FZ1200MachineTemplate(),sim=new FZ1200ProcessSimulation(model.root,model);assert.ok(FZ1200_SIMULATION_STAGES.includes('Turn pile 180°'));sim.start();sim.update(sim.startAt+2500);let state=sim.state();assert.equal(state.clamped,true);sim.update(sim.startAt+5200);state=sim.state();assert.ok(state.turnAngleDeg>100);sim.update(sim.startAt+6800);state=sim.state();assert.equal(state.airingActive,true);sim.update(sim.startAt+8000);state=sim.state();assert.equal(state.joggingActive,true);sim.dispose();model.dispose();});
