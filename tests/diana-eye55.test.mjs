import test from 'node:test';import assert from 'node:assert/strict';import * as THREE from 'three';
import {DianaEye55MachineTemplate} from '../frontend/src/diana-eye55.js';import {DianaEye55ProcessSimulation,DIANA_EYE55_SIMULATION_STAGES} from '../frontend/src/simulation-diana-eye55.js';import {DIANA_EYE55_SPEC} from '../frontend/src/data/dimensions-diana-eye55.js';import {DIANA_EYE55_TAXONOMY} from '../frontend/src/data/taxonomy-diana-eye55.js';import {DIANA_EYE55_TECHNICAL_SOURCES} from '../frontend/src/data/sources-diana-eye55.js';
test('DIANA EYE 55 keeps BMJ identity and official family limits explicit',()=>{assert.equal(DIANA_EYE55_SPEC.assetId,'BMJ-MCH-0019');assert.equal(DIANA_EYE55_SPEC.serial,'MP.FBA0-00058');assert.equal(DIANA_EYE55_SPEC.year,2023);assert.equal(DIANA_EYE55_SPEC.maxSpeedMMin,300);assert.deepEqual(DIANA_EYE55_SPEC.materialGsm,[90,650]);assert.deepEqual(DIANA_EYE55_SPEC.maxSheetM,[.550,.500]);assert.deepEqual(DIANA_EYE55_SPEC.minSheetHeidelbergM,[.070,.070]);assert.deepEqual(DIANA_EYE55_SPEC.minSheetMasterworkCurrentM,[.090,.090]);assert.ok(DIANA_EYE55_TECHNICAL_SOURCES.filter(s=>s.authority==='primary').length>=3);assert.ok(DIANA_EYE55_TECHNICAL_SOURCES.some(s=>s.id==='DIANA-EVIDENCE-BOUNDARY'));});
test('DIANA geometry contains feed suction inspection optical processing reject and delivery sections',()=>{const model=new DianaEye55MachineTemplate(),box=new THREE.Box3().setFromObject(model.root);assert.ok(!box.isEmpty());assert.ok(box.min.y>=-0.01);for(const id of ['diana55-feeder','diana55-transport','diana55-inspection','diana55-camera','diana55-light','diana55-processing','diana55-reject','diana55-delivery','diana55-access'])assert.ok(model.findNode(id),id);assert.equal(model.meshes.filter(m=>m.userData.cameraBay).length,4);assert.ok(model.meshes.filter(m=>m.userData.inspectionLight).length>=8);model.setExteriorOpen(true);assert.ok(model.root.userData.exteriorHiddenCount>=6);model.dispose();});
test('DIANA taxonomy is a mapped six-level tree',()=>{assert.deepEqual([...new Set(DIANA_EYE55_TAXONOMY.map(n=>n.level))].sort(),[1,2,3,4,5,6]);assert.equal(new Set(DIANA_EYE55_TAXONOMY.map(n=>n.id)).size,DIANA_EYE55_TAXONOMY.length);for(const n of DIANA_EYE55_TAXONOMY.filter(n=>n.level>1))assert.ok(DIANA_EYE55_TAXONOMY.some(p=>p.id===n.parentId),n.id);const model=new DianaEye55MachineTemplate();for(const n of DIANA_EYE55_TAXONOMY.filter(n=>n.verified&&n.meshRefs.length))assert.ok(model.resolveTaxonomyNode(n.id),n.id);model.dispose();});
test('DIANA simulation performs scan and clearly marks reject behavior as demonstration only',()=>{const model=new DianaEye55MachineTemplate(),sim=new DianaEye55ProcessSimulation(model.root,model);assert.ok(DIANA_EYE55_SIMULATION_STAGES.includes('LED illumination + camera scan'));sim.start();sim.update(sim.startAt+3400);let state=sim.state();assert.equal(state.scanActive,true);assert.equal(state.demoRejectOnly,true);sim.update(sim.startAt+6100);state=sim.state();assert.equal(state.demoRejectActive,true);sim.update(sim.startAt+8200);state=sim.state();assert.ok(state.completed+state.rejectedDemo>0);assert.ok(state.pileSheetsVisible+state.rejectSheetsVisible>0);sim.dispose();model.dispose();});
