import test from 'node:test';import assert from 'node:assert/strict';import * as THREE from 'three';
import {UpgLy300MachineTemplate} from '../frontend/src/upg-ly300.js';import {UpgLy300ProcessSimulation,UPG_LY300_SIMULATION_STAGES} from '../frontend/src/simulation-upg-ly300.js';import {UPG_LY300_SPEC} from '../frontend/src/data/dimensions-upg-ly300.js';import {UPG_LY300_TAXONOMY} from '../frontend/src/data/taxonomy-upg-ly300.js';import {UPG_LY300_TECHNICAL_SOURCES} from '../frontend/src/data/sources-upg-ly300.js';
test('UPG LY300 identity and OEM dimensions stay explicit',()=>{assert.equal(UPG_LY300_SPEC.assetId,'BMJ-MCH-0024');assert.equal(UPG_LY300_SPEC.oemReferenceModel,'LQ-UPG LY300');assert.deepEqual(UPG_LY300_SPEC.envelopeM,[4.230,.720,1.700]);assert.equal(UPG_LY300_SPEC.weightKg,800);assert.deepEqual(UPG_LY300_SPEC.materialWidthMm,[50,250]);assert.equal(UPG_LY300_SPEC.lineSpeedMaxMMin,70);assert.equal(UPG_LY300_SPEC.printhead,'Ricoh G5');assert.equal(UPG_LY300_SPEC.cameraInspection,'2K line scan');assert.ok(UPG_LY300_TECHNICAL_SOURCES.filter(s=>s.authority==='primary').length>=2);assert.ok(UPG_LY300_TECHNICAL_SOURCES.some(s=>s.id==='UPG-LY300-EVIDENCE-BOUNDARY'));});
test('UPG LY300 geometry follows feeder print UV camera reject collection train',()=>{const model=new UpgLy300MachineTemplate(),box=new THREE.Box3().setFromObject(model.root);assert.ok(!box.isEmpty());assert.ok(box.min.y>=-0.01);for(const id of ['ly300-feeder','ly300-transport','ly300-print','ly300-ink','ly300-uv','ly300-camera','ly300-reject','ly300-collect','ly300-control','ly300-access'])assert.ok(model.findNode(id),id);assert.ok(model.meshes.some(m=>m.userData.nozzleArray));assert.ok(model.meshes.some(m=>m.userData.uvLamp));model.setExteriorOpen(true);assert.ok(model.root.userData.exteriorHiddenCount>=4);model.dispose();});
test('UPG LY300 taxonomy is a mapped six-level tree',()=>{assert.deepEqual([...new Set(UPG_LY300_TAXONOMY.map(n=>n.level))].sort(),[1,2,3,4,5,6]);assert.equal(new Set(UPG_LY300_TAXONOMY.map(n=>n.id)).size,UPG_LY300_TAXONOMY.length);for(const n of UPG_LY300_TAXONOMY.filter(n=>n.level>1))assert.ok(UPG_LY300_TAXONOMY.some(p=>p.id===n.parentId),n.id);const model=new UpgLy300MachineTemplate();for(const n of UPG_LY300_TAXONOMY.filter(n=>n.verified&&n.meshRefs.length))assert.ok(model.resolveTaxonomyNode(n.id),n.id);model.dispose();});
test('UPG LY300 simulation separates printing UV cure camera and demo rejection',()=>{const model=new UpgLy300MachineTemplate(),sim=new UpgLy300ProcessSimulation(model.root,model);assert.ok(UPG_LY300_SIMULATION_STAGES.includes('Ricoh G5 variable-data print'));sim.start();sim.update(sim.startAt+2400);let state=sim.state();assert.equal(state.printingActive,true);sim.update(sim.startAt+3300);state=sim.state();assert.equal(state.uvActive,true);sim.update(sim.startAt+4200);state=sim.state();assert.equal(state.cameraActive,true);assert.equal(state.demoRejectOnly,true);sim.update(sim.startAt+7200);state=sim.state();assert.ok(state.completed+state.rejectedDemo>0);sim.dispose();model.dispose();});
