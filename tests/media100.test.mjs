import test from 'node:test';import assert from 'node:assert/strict';import * as THREE from 'three';
import {Media100MachineTemplate} from '../frontend/src/media100.js';import {Media100ProcessSimulation,MEDIA100_SIMULATION_STAGES} from '../frontend/src/simulation-media100.js';import {MEDIA100_SPEC,MEDIA100_SPEC_FGM3} from '../frontend/src/data/dimensions-media100.js';import {media100TaxonomyFor} from '../frontend/src/data/taxonomy-media100.js';import {MEDIA100_TECHNICAL_SOURCES} from '../frontend/src/data/sources-media100.js';
test('MEDIA 100 II keeps BMJ identities separate and family limits evidence bounded',()=>{assert.equal(MEDIA100_SPEC.assetId,'BMJ-MCH-0016');assert.equal(MEDIA100_SPEC.serial,'0341 06903');assert.equal(MEDIA100_SPEC_FGM3.assetId,'BMJ-MCH-0018');assert.equal(MEDIA100_SPEC_FGM3.serial,'0341 142 07');assert.equal(MEDIA100_SPEC.workingWidthM,1);assert.equal(MEDIA100_SPEC.nominalRunSpeedMMin,300);assert.equal(MEDIA100_SPEC.inchingSpeedMMin,20);assert.ok(MEDIA100_TECHNICAL_SOURCES.some(s=>s.authority==='primary-archive'));assert.ok(MEDIA100_TECHNICAL_SOURCES.some(s=>s.id==='MEDIA100-EVIDENCE-BOUNDARY'));});
test('both BMJ MEDIA 100 II assets instantiate the folder-gluer process train',()=>{for(const assetId of ['BMJ-MCH-0016','BMJ-MCH-0018']){const model=new Media100MachineTemplate(assetId),box=new THREE.Box3().setFromObject(model.root);assert.equal(model.root.userData.assetId,assetId);assert.ok(!box.isEmpty());assert.ok(box.min.y>=-0.01);for(const id of ['media100-feeder','media100-prefold','media100-forming','media100-glue','media100-final','media100-compression','media100-drive','media100-access'])assert.ok(model.findNode(id),id);assert.ok(model.meshes.filter(m=>m.userData.rotor).length>=15);model.dispose();}});
test('MEDIA 100 taxonomy is a complete six-level tree for FGM 1 and FGM 3',()=>{for(const assetId of ['BMJ-MCH-0016','BMJ-MCH-0018']){const taxonomy=media100TaxonomyFor(assetId);assert.deepEqual([...new Set(taxonomy.map(n=>n.level))].sort(),[1,2,3,4,5,6]);assert.equal(new Set(taxonomy.map(n=>n.id)).size,taxonomy.length);for(const n of taxonomy.filter(n=>n.level>1))assert.ok(taxonomy.some(p=>p.id===n.parentId),n.id);const model=new Media100MachineTemplate(assetId);for(const n of taxonomy.filter(n=>n.verified&&n.meshRefs.length))assert.ok(model.resolveTaxonomyNode(n.id),n.id);model.dispose();}});
test('MEDIA 100 simulation folds, applies glue, compresses and delivers without random mechanism motion',()=>{const model=new Media100MachineTemplate(),sim=new Media100ProcessSimulation(model.root,model);assert.ok(MEDIA100_SIMULATION_STAGES.includes('Final folding / trombone'));sim.start();sim.update(sim.startAt+3000);let state=sim.state();assert.equal(state.foldingActive,true);sim.update(sim.startAt+4700);state=sim.state();assert.equal(state.glueApplying,true);sim.update(sim.startAt+7200);state=sim.state();assert.equal(state.compressionActive,true);sim.update(sim.startAt+9200);state=sim.state();assert.ok(state.completed>0);assert.ok(state.pileSheetsVisible>0);sim.dispose();model.dispose();});
